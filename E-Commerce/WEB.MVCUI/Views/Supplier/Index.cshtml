@model List<Supplier>

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Tedarikçi Listesi</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <div class="container mt-4">
        <h2 class="text-center">Tedarikçi Listesi</h2>

        <div class="mb-3">
            <a href="/Supplier/Add" class="btn btn-success">+ Tedarikçi Ekle</a>
        </div>

        <table class="table table-bordered table-striped">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Tedarikçi Adı</th>
                    <th>İletişim Adı</th>
                    <th>Adres</th>
                    <th>Şehir</th>
                    <th>Ülke</th>
                    <th>Posta Kodu</th>
                    <th>Telefon</th>
                    <th>Güncelle</th>
                    <th>Sil</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.SupplierID</td>
                        <td>@(item.SupplierName ?? "-")</td>
                        <td>@(item.ContactName ?? "-")</td>
                        <td>@(item.Address ?? "-")</td>
                        <td>@(item.City ?? "-")</td>
                        <td>@(item.Country ?? "-")</td>
                        <td>@(item.PostalCode ?? "-")</td>
                        <td>@(item.Phone ?? "-")</td>
                        <td>
                            <a href="@Url.Action("Edit", "Supplier", new { id = item.SupplierID })" class="btn btn-warning btn-sm">Güncelle</a>
                        </td>
                        <td>
                            <a href="@Url.Action("Delete", "Supplier", new { id = item.SupplierID })" class="btn btn-danger btn-sm" onclick="return confirm('Bu tedarikçiyi silmek istediğinize emin misiniz?');">Sil</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
